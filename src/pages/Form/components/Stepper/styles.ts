import { createStyles } from '@mantine/core';

export const useStyles = createStyles(() => ({
  StepperWrapper: {
    width: '100%',
    padding: '0 2rem'
  },
  StepperContentWrapper: {
    marginTop: '-0.5rem'
  }
}));
